<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.imoveis">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Imoveis">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListViewRealtorActivity">
            <intent-filter>
                <action android:name="android.intent.action" />

                <category android:name="android.intent.category" />
            </intent-filter>
        </activity>
        <activity android:name=".RealtorRegisterActivity"
            android:parentActivityName=".ListViewRealtorActivity">
            <intent-filter>
                <action android:name="android.intent.action" />

                <category android:name="android.intent.category" />
            </intent-filter>
        </activity>
        <activity android:name=".PropertyRegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action" />

                <category android:name="android.intent.category" />
            </intent-filter>
        </activity>
        <activity android:name=".ListViewPropertyActivity">
            <intent-filter>
                <action android:name="android.intent.action" />

                <category android:name="android.intent.category." />
            </intent-filter>
        </activity>
        <activity android:name=".AboutActivity"
          android:parentActivityName=".ListViewRealtorActivity">
            <intent-filter>
                <action android:name="android.intent.action" />

                <category android:name="android.intent.category." />
            </intent-filter>
        </activity>

    </application>

</manifest>